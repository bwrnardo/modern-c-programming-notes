#include <stdio.h>

int main(void)
{
    int n;
    if (n >= 1 <= 10)
        printf("n is between 1 and 10\n"); 
}
// this statement is legal but always will be equal to 1, because (n >= 1) will be either 1 or 0, which less than or equal to 10