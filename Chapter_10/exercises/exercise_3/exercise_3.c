/*

Suppose that a program has only one function (main). How many different variables names i could this program contain?

It can have infinite blocks declaring i, but main would be only able to use 2, the global variable and the i variable declared in the scope
 

*/